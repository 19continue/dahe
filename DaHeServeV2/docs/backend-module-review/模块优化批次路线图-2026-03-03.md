# DaHeServeV2 模块优化批次路线图（最终版）
- 更新时间：2026-03-05
- 目标：在统一新鉴权模型下，按优先级完成除 `auth` 外各模块优化与前端迁移对齐。

## 全局约束
1. 严格遵守：`V2项目要求与规范(必读).md`。
2. 控制层只做协议转换，业务逻辑必须下沉到 service。
3. 鉴权通过统一机制处理，禁止在业务控制器内硬编码角色判断。
4. 每完成一个模块必须独立提交一次 `git`。
5. 涉及 API 变更时必须同步迁移文档并标记前端迁移状态。

## 批次进展
1. 批次1：`session + miniapp`，已完成。
2. 批次2：`user`，已完成。
3. 批次3：`assets`，已完成。
4. 批次4：`farm -> field -> seed`，已完成。
5. 批次5：`dynamic + meta + crop + export + company + amap + oplog`，进行中。

## 批次5状态
1. `dynamic`：已完成。
2. `meta`：已完成。
3. `crop`：已完成。
4. `export`：已完成。
5. `company`：已完成。
6. `amap`：已完成（本次）。
7. `oplog`：已完成（本次）。

## 当前执行顺序
1. 批次5已完成，等待下一批次任务。

## 完成门槛
1. 模块代码结构优化完成并可编译。
2. 模块解读文档与待改进文档更新为最终版。
3. 若有 API 变化，迁移文档与前端迁移状态同步完成。
4. 记录模块级 `git` 提交。
