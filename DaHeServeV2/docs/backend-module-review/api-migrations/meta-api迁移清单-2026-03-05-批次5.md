# meta API 迁移清单（批次5）
- 更新时间：2026-03-05
- 模块：`meta`

## 结论
本轮无路由变更，属于行为增强；前端无需改路径。

## 后端变更
1. 元数据选项接口显式登录校验
- 路由：`/api/v2/meta/options/**`
- 行为：未登录返回 `UNAUTHORIZED`。

2. 词典全量替换语义调整
- 路由：`POST /api/v2/admin/terminology-dict/replace-all`
- 行为：由“先删后插”改为“增量写入 + 差异删除”，降低空窗风险。

3. 词典唯一冲突提示统一
- 路由：`POST/PUT /api/v2/admin/terminology-dict*`
- 行为：重复 `source_text` 返回明确提示“源术语已存在”。

## 前端迁移状态（DaHeAdminV2）
1. 当前状态：无需代码改动。
2. 建议优化：
- 直接展示后端校验 message（尤其是“源术语已存在”）。

## 迁移标记
- `后端`：已完成
- `前端`：无需改动（可选提示优化）
