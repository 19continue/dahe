# seed API 迁移清单（批次4）
- 更新时间：2026-03-05
- 模块：`seed`

## 结论
本轮为非破坏性升级，无需前端强制迁移。

## 后端变更
1. `POST /api/v2/seed-batches/{id}/tests`
- 新增可选字段：`requestKey`（字符串，最长 64）。
- 作用：作为幂等键，重复请求返回同一条检测记录。

2. 数据库
- `seed_quality_test` 新增 `request_key`。
- 新增唯一索引：`uk_seed_test_batch_request_key(batch_id, request_key)`。

## 前端迁移状态（DaHeAdminV2）
1. 当前状态：无需立即改动。
- 原有调用不传 `requestKey` 仍可正常使用。
- 建议后续在“检测新增”请求接入 `requestKey`（如 uuid），提升重试安全性。

2. 迁移标记
- `后端`：已完成
- `前端`：可选优化（未阻塞）

## 验证
1. 同一批次同 `requestKey` 重复提交：返回同一条记录。
2. 未传 `requestKey`：保持历史行为。
