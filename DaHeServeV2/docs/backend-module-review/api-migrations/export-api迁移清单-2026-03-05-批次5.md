# export API迁移清单（2026-03-05，批次5）

## 1. 结论
本批次 `export` 模块属于“内部重构”，对外 API 路径与请求参数保持不变。

## 2. 后端变更摘要
1. `ExportController` 瘦身，仅保留协议层。
2. 导出编排下沉到 `ExportCsvFacadeServiceImpl`。
3. 模板字段 JSON 校验增强（结构、编码、数量）。
4. 模板/词典唯一键冲突提示语义化。
5. 修复并重写 `schema-export.sql`（可直接执行）。

## 3. API兼容性核对
1. `GET /api/v2/admin/exports/farm-records.csv`：无变更。
2. `GET /api/v2/admin/exports/seed-tests.csv`：无变更。
3. `GET/POST/PUT/DELETE /api/v2/admin/export-templates`：无路径变更，新增更明确的冲突提示。
4. `GET/POST/PUT/DELETE /api/v2/admin/export-dicts`：无路径变更，新增更明确的冲突提示。

## 4. 前端迁移状态（DaHeAdminV2）
1. API 路径迁移：不需要。
2. 调用参数迁移：不需要。
3. 建议动作（可选）：
- 对后端返回的校验提示直接透传展示（避免继续显示笼统“系统内部错误”）。

## 5. 状态
`export`：已完成迁移核对，标记为“无需前端代码变更”。
