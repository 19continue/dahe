# crop API 迁移清单（批次5）
- 更新时间：2026-03-05
- 模块：`crop`

## 结论
本轮无路由变更，属于行为增强；前端无需修改接口路径。

## 后端变更
1. 删除接口新增引用保护
- 路由：`DELETE /api/v2/crops/{id}`
- 行为：若被 `farm_process_template.crop_id` 引用，返回业务提示“作物已被流程模板引用，无法删除”。

2. 新增/更新唯一冲突语义统一
- 路由：`POST /api/v2/crops`、`PUT /api/v2/crops/{id}`
- 行为：分类重名、同分类下品种重名统一返回明确提示。

3. 路由授权收口
- 路由：`/api/v2/crops/**`
- 行为：后台按 `/crop-manage` 权限码授权；小程序账号拒绝访问。

## 前端迁移状态（DaHeAdminV2）
1. 当前状态：无需代码改动（接口路径与字段保持兼容）。
2. 建议优化：
- 在删除失败时直接展示后端返回的业务提示，区分“被引用无法删除”和通用失败。

## 迁移标记
- `后端`：已完成
- `前端`：无需改动（可选提示优化）
