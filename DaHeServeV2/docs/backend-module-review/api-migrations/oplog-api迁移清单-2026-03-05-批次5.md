# `oplog` API 迁移清单（2026-03-05，批次5）

## 1. 结论
本次 `oplog` 优化不涉及接口路径变更，`DaHeAdminV2` 无需修改请求地址。

## 2. 后端变更
1. `OperationLogController` 增加 `@AdminMenuCode("/operation-logs")`。
2. `operation_log` 新增字段：`undo_fail_reason`。
3. 撤销失败时返回逻辑保持原接口语义，同时将失败原因持久化。

## 3. 前端影响
1. 现有页面可继续使用原有接口。
2. 如需增强可观测性，可在列表中新增 `undoFailReason` 展示（本次不强制）。
3. 迁移状态：`已核对，路径无变更`。

## 4. 验证
1. 后端编译：`mvn -q -DskipTests compile` 已通过。
