# DaHeServeV2 `farm` 模块待改进的点（总版）

- 更新时间：2026-03-04
- 对应解读文档：`docs/backend-module-review/modules/farm-模块解读.md`

## 已完成（本批次）

1. 记录写链路事务化
- `create/update/delete` 已纳入事务边界。

2. 模板删除级联一致性
- 模板删除前级联删步骤，避免孤儿数据。

3. 模块鉴权切换
- `farm-process`、`record-policy` 已注解鉴权。
- `farm-record` 已去除固定角色判断，改为后台菜单权限 + 小程序语义化权限。

## P0

1. 记录查询数据域收口
- 现状：`page/grouped/detail` 仍偏通用，需补充小程序数据域限制策略。
- 目标：后台可看全量，小程序按授权范围（本人/田块）可见。

2. 小程序专属接口拆分
- 现状：`farm-record` 同时承载后台与小程序写入口。
- 目标：后续将小程序编排接口迁移到 `modules/miniapp`，`farm` 保留领域能力。

## P1

1. 记录级权限下沉服务化
- 现状：`resolvePermission` 仍在控制器。
- 目标：抽离授权域服务，提高复用与可测性。

2. 分组统计性能优化
- 现状：存在 N+1 风险。
- 目标：改为一次性聚合查询或窗口函数方案。

## P2

1. 状态映射配置化
- 将 `growth_stage -> field/cycle status` 从控制器迁出，避免策略硬编码扩散。

2. 回归测试补齐
- 增加鉴权、策略窗口、事务回滚、状态联动的集成测试矩阵。
